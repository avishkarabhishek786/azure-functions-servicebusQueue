{
  "name": "azure-functions-servicebusqueue",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "func start",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "@azure/service-bus": "^7.9.0"
  },
  "devDependencies": {
    "@azure/functions": "^3.0.0",
    "@types/node": "^16.x",
    "dotenv": "^16.3.1",
    "rimraf": "^5.0.0",
    "typescript": "^4.0.0"
  }
}
